username = input('What is your username?')
password = input('What is your password?')

password_length = len(password) * '*'
password_letters = int(len(password))

print(f'Hi {username}, your password {password_length} is {password_letters} letters long')
